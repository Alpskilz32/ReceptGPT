{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Open localhost",
            "url": "http://localhost:8080" // Adjust the port number if necessary
        }
    ]
}
